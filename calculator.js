/**
 * Returns the sum of a and b
 */
let add = (a, b) => {
    return a + b;
};

/**
 * Returns the difference of a and b
 */
let subtract = (a, b) => {
    return a - b;
};

/**
 * Returns the product of a and b
 */
let multiply = (a, b) => {
    return a * b;
};
